# Build artifacts and fetched dependencies (large, regenerable)
build/
build_release/
build_*/
external/

# Sweep outputs (large solution dumps); keep scripts + result tables
sweeps/out/
sweeps/*.out

# Reference solutions / outputs (24 MB each)
*reference_solution_*.txt
ballistic_test*.txt
*.out
logs/

# Editor / OS
*.o
*.swp
.DS_Store

# sweep run logs
sweeps/*.log

# benchmark work dir (regenerable); keep golden refs
sweeps/bench/work/
